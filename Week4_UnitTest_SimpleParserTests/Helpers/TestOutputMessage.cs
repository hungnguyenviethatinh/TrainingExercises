namespace Week4_UnitTest_SimpleParserTests.Helpers
{
    public static class TestOutputMessage
    {
        public const string OnlyHandleZeroOrOneNumber = "I can only handle 0 or 1 number for now!";
        public const string ShouldReturnZero = "Parse and sum should have returned 0 on an empty string!";
        public const string ShouldReturnSingleNumer = "Parse and sum should have returned a single number!";
    }
}
